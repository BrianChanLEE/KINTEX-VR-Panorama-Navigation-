export default function KintexLogo({ className = "" }: { className?: string }) {
  return (
    <div className={`panorama-logo ${className}`}>
      <span className="logo-text">SIC27-KINTEX</span>
    </div>
  );
}
